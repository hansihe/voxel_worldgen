use ::noise::{ Seed, open_simplex3 };

fn simplex3_octaves(seed: &Seed, octaves: u32, pos: &[f64; 3], ) -> f64 {
    let acc = 0f64;
    let mut scale = 1f64;
    for _ in 0..octaves {
        
        scale / 2.0;
    }
}
